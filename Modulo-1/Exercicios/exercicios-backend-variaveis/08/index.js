const raioDoCirculo = 1;
let compriDoCirculo;
let areaDoCirculo;

compriDoCirculo = (2 * Math.PI * raioDoCirculo).toFixed(2);
areaDoCirculo = (Math.PI * raioDoCirculo ** 2).toFixed(2);
console.log(compriDoCirculo);
console.log(areaDoCirculo);

